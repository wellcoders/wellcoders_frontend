import { DragelementDirective } from './dragelement.directive';

describe('DragelementDirective', () => {
  it('should create an instance', () => {
    const directive = new DragelementDirective();
    expect(directive).toBeTruthy();
  });
});
